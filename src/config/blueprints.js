module.exports = {
  /**
   * @property {number} defaultLimit Default limit property can be used anytime to always
   * paginate at the api level so we don't end up with memory leaks per instance
   */
  defaultLimit: 25,
};
